#include <stdio.h>

void main0702() {
	// 산술연산자

	int x = 5, y = 3;

	int result1 = x + y;
	int result2 = x - y;
	int result3 = x * y;
	int result4 = x / y;
	int result5 = x % y;

	printf("result : %d, %d, %d, %d, %d\n", result1, result2, result3, result4, result5);
}